name: Deploy Environments

on:
  workflow_dispatch:       # ✅ CORRECT top-level trigger

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "Hello, workflow_dispatch works!"